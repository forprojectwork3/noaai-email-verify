<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ─── Permissions ─── -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AILectureSummarizer"
        android:usesCleartextTraffic="true">

        <!-- ─── Splash (entry point) ─── -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:noHistory="true"
            android:theme="@style/Theme.AILectureSummarizer">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ─── Login ─── -->
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.AILectureSummarizer"
            android:windowSoftInputMode="adjustResize">
            <intent-filter android:label="NOA AI Auth Handshake">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:host="auth-callback"
                    android:scheme="myapp" />
            </intent-filter>
        </activity>

        <!-- ─── Register / Sign-Up ─── -->
        <activity
            android:name=".RegisterActivity"
            android:exported="false"
            android:theme="@style/Theme.AILectureSummarizer"
            android:windowSoftInputMode="adjustResize" />

        <!-- ─── Forgot Password ─── -->
        <activity
            android:name=".ForgotPasswordActivity"
            android:exported="false"
            android:theme="@style/Theme.AILectureSummarizer"
            android:windowSoftInputMode="adjustResize" />

        <!-- ─── Reset Password (handles deep link from Supabase email) ─── -->
        <!--
            Supabase password-reset emails contain a link redirected to:
              myapp://reset-password#access_token=<JWT>&type=recovery&...
            This intent-filter opens ResetPasswordActivity when that link is clicked.
        -->
        <activity
            android:name=".ResetPasswordActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.AILectureSummarizer"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <!-- Custom scheme deep link: myapp://reset-password -->
                <data
                    android:host="reset-password"
                    android:scheme="myapp" />
            </intent-filter>
        </activity>

        <!-- ─── Main (Lecture Summarizer) ─── -->
        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:exported="true"
            android:hardwareAccelerated="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.AILectureSummarizer"
            android:windowSoftInputMode="adjustResize">
            <!-- Deep-link: "Open NOA AI" button in welcome email → opens MainActivity directly -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="noaai.dpdns.org" android:pathPrefix="/open" />
            </intent-filter>
        </activity>

        <!-- ─── Chatbot ─── -->
        <activity
            android:name=".ChatbotActivity"
            android:exported="false"
            android:theme="@style/Theme.AILectureSummarizer"
            android:windowSoftInputMode="adjustResize" />

        <!-- ─── Profile ─── -->
        <activity
            android:name=".ProfileActivity"
            android:exported="false"
            android:theme="@style/Theme.AILectureSummarizer"
            android:windowSoftInputMode="adjustResize" />

    </application>

</manifest>
